// import type { NextConfig } from "next"; // Removed type import

const nextConfig = { // Removed explicit NextConfig type
  /* config options here */
};

module.exports = nextConfig; // Changed export to module.exports
